import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // Brand — green (primary CTA, screens layer)
  static const Color primary = Color(0xFF0E7C66);
  static const Color primaryDark = Color(0xFF085041);
  static const Color primarySoft = Color(0xFFD7F0E7);
  static const Color onPrimary = Color(0xFFFFFFFF);

  // Lessons — purple
  static const Color lesson = Color(0xFF6C5DD3);
  static const Color lessonSoft = Color(0xFFEDEAFB);
  static const Color onLesson = Color(0xFFFFFFFF);

  // Real Situation Mode — orange (KEY differentiator)
  static const Color scenario = Color(0xFFE56A3C);
  static const Color scenarioSoft = Color(0xFFFCE5DA);
  static const Color onScenario = Color(0xFFFFFFFF);

  // Saved phrases / progress — amber
  static const Color saved = Color(0xFFE0A126);
  static const Color savedSoft = Color(0xFFFBEFD2);
  static const Color onSaved = Color(0xFF1F1F1D);

  // Neutrals
  static const Color background = Color(0xFFFAF9F5);
  static const Color surface = Color(0xFFFFFFFF);
  static const Color surfaceAlt = Color(0xFFF2F0E9);
  static const Color outline = Color(0xFFE5E2D8);

  // Text
  static const Color textPrimary = Color(0xFF1F1F1D);
  static const Color textSecondary = Color(0xFF5C5B55);
  static const Color textTertiary = Color(0xFF93918A);

  // Status
  static const Color error = Color(0xFFB3261E);
  static const Color onError = Color(0xFFFFFFFF);
  static const Color success = Color(0xFF2E7D32);
}
